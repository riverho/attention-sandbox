{
  "name": "attention-sandbox",
  "description": "Minimal folder sandbox for Pi. Blocks agent from accessing unmapped paths with a friendly note suggesting how to expand the sandbox.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.6.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  }
}
